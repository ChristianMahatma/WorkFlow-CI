name: Smartphone_Price_Prediction_Christian

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: string
        default: "smartphone_preprocessing/smartphone_preprocessed.csv"
      target:
        type: string
        default: "Price"
    command: "python Membangun_model/modeling_tuning.py"